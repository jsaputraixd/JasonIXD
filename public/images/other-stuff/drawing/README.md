# Drawing

Add drawings here (sketches, ink, digital, etc.), then register each file under `drawing` → `items` in `data/otherStuff.js`.

```js
{
  id: "sketch-name",
  src: "/images/other-stuff/drawing/sketch-name.jpg",
  alt: "Description",
  caption: "Optional label",
}
```
