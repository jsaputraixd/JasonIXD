/** Matches `CRTOverlay` bezel + corner-cap geometry. */
export const CRT_BEZEL_RADIUS = "min(80px, 5.5vmin)";

/** Horizontal inset so bottom-row labels clear the curved corner caps. */
export const CRT_FOOTER_CORNER_INSET = "clamp(18px, 6vmin, 88px)";

/** Extra bottom padding so text sits above the inner bezel arc. */
export const CRT_FOOTER_BOTTOM_PAD = "clamp(7px, 1.2vmin, 14px)";
